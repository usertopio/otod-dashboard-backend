version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: dockerFile # <-- move under build
    ports:
      - "5000:5000"
    env_file: .env
    volumes:
      - ./logs:/app/logs
