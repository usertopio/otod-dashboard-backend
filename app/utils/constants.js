const OPERATIONS = {
  INSERT: "INSERT",
  UPDATE: "UPDATE",
  ERROR: "ERROR",
};

const STATUS = {
  SUCCESS: "SUCCESS",
  INCOMPLETE: "INCOMPLETE",
};

const NEWS_CONFIG = {
  DEFAULT_TARGET_COUNT: 1000,
  DEFAULT_MAX_ATTEMPTS: 10,
  DEFAULT_TOTAL_RECORDS: 1000,
  DEFAULT_PAGE_SIZE: 100,
};

const FARMERS_CONFIG = {
  DEFAULT_TARGET_COUNT: 1000,
  DEFAULT_MAX_ATTEMPTS: 10,
  DEFAULT_TOTAL_RECORDS: 2000,
  DEFAULT_PAGE_SIZE: 500,
};

const COMMUNITIES_CONFIG = {
  DEFAULT_TARGET_COUNT: 200,
  DEFAULT_MAX_ATTEMPTS: 5,
  DEFAULT_TOTAL_RECORDS: 200,
  DEFAULT_PAGE_SIZE: 100,
};

const MERCHANTS_CONFIG = {
  DEFAULT_TARGET_COUNT: 50,
  DEFAULT_MAX_ATTEMPTS: 5,
  DEFAULT_TOTAL_RECORDS: 50,
  DEFAULT_PAGE_SIZE: 50,
};

const OPERATIONS_CONFIG = {
  DEFAULT_TARGET_COUNT: 100,
  DEFAULT_MAX_ATTEMPTS: 5,
  DEFAULT_TOTAL_RECORDS: 100,
  DEFAULT_PAGE_SIZE: 50,
};

const GAP_CONFIG = {
  DEFAULT_TARGET_COUNT: 500,
  DEFAULT_MAX_ATTEMPTS: 5,
  DEFAULT_TOTAL_RECORDS: 1000,
  DEFAULT_PAGE_SIZE: 500,
};

const WATER_CONFIG = {
  DEFAULT_TARGET_COUNT: 100,
  DEFAULT_MAX_ATTEMPTS: 5,
  DEFAULT_TOTAL_RECORDS: 100,
  DEFAULT_PAGE_SIZE: 50,
};

const SUBSTANCE_CONFIG = {
  DEFAULT_TARGET_COUNT: 100,
  DEFAULT_MAX_ATTEMPTS: 5,
  DEFAULT_TOTAL_RECORDS: 100,
  DEFAULT_PAGE_SIZE: 50,
};

// 🔧 ADD: Crops configuration
const CROPS_CONFIG = {
  DEFAULT_TARGET_COUNT: 10, // 🔧 Start small for testing
  DEFAULT_MAX_ATTEMPTS: 3, // 🔧 Small number for testing
  DEFAULT_TOTAL_RECORDS: 518, // 🔧 From your current crops config
  DEFAULT_PAGE_SIZE: 500, // 🔧 From your current crops config
  DEFAULT_CROP_YEAR: 2024, // 🔧 Default crop year
};

// 🔧 IMPORTANT: Make sure CROPS_CONFIG is exported
module.exports = {
  OPERATIONS,
  STATUS,
  NEWS_CONFIG,
  FARMERS_CONFIG,
  COMMUNITIES_CONFIG,
  MERCHANTS_CONFIG,
  OPERATIONS_CONFIG,
  GAP_CONFIG,
  WATER_CONFIG,
  SUBSTANCE_CONFIG,
  CROPS_CONFIG, // 🔧 MUST BE INCLUDED HERE
};
